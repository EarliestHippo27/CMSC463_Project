.csv annotation is simply the original annotations.csv with all columns removed that aren't the
file name or the relevant label.

Provided is the model which we trained and used to generate the data with.